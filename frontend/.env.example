# Base URL of the InsightFlow backend API. Leave blank to use Next.js rewrites
# (calls are proxied to http://localhost:8000 in dev). Set an absolute URL to
# talk to the backend directly (useful when frontend/backend are separate hosts).
NEXT_PUBLIC_API_BASE_URL=
