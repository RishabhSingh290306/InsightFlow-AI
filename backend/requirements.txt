# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Data layer (abstracted — see app/core/database.py for the swap point)
sqlmodel==0.0.22
psycopg2-binary==2.9.10  # Postgres driver for SQLModel engine
alembic==1.13.3

# Config & validation
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0  # required by pydantic EmailStr

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pin: passlib 1.7.4 is incompatible with bcrypt>=4.1
python-multipart==0.0.12

# Task queue (AI workflows — wired later in Sprint 1+)
celery==5.4.0
redis==5.1.1

# Data parsing — dataset metadata (row/column counts) on upload
pandas==2.2.3
openpyxl==3.1.5  # Excel engine for pandas.read_excel

# HTTP client (OpenRouter, OAuth token exchange)
httpx==0.27.2

# Managed file storage (Supabase Storage — swap point, see app/core/storage.py).
# Only needed when STORAGE_BACKEND=supabase. ~= allows any 2.x patch.
supabase~=2.10

# Tests
pytest==8.3.2

# SQL Generation sandbox (read-only query execution over the in-memory frame)
duckdb==1.5.4
sqlglot==30.12.0
